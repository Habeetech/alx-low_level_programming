#include "main.h"

/**
 * print_count - prints the number of argument
 * followed by a new line
 * @argc: argument count
 * Return: always 0.
 */

int print_count(int argc)
{
	_putchar(argc + '0');
	_putchar('\n');
	return (0);
}
